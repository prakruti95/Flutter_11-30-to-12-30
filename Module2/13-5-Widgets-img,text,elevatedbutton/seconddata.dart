import 'package:flutter/material.dart';

class Tops2 extends StatelessWidget
{
  const Tops2({super.key});

  @override
  Widget build(BuildContext context)
  {
    return Scaffold
      (
        appBar: AppBar(title: Text("Tops 2"),),
        body: Center
          (

          ),
      );
  }
}
